[project]
name = "private-cio"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-genai>=2.22.0",
    "ollama>=0.6.2",
    "pandas>=3.0.5",
    "requests>=2.34.2",
    "streamlit>=1.63.0",
    "yfinance>=1.7.0",
]

[project.scripts]
private-cio = "private_cio:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
